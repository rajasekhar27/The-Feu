/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ["fue-website-cdn.sgp1.cdn.digitaloceanspaces.com"],
  },
};

module.exports = nextConfig;
